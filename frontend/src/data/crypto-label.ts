interface CryptoLabel {
    symbol: string;
    name: string;
    value: number;
    change: number;
    icon: string; // later change this to an actual icon
    isActive: boolean;
}
export default CryptoLabel;